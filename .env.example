# Copy to .env. Vite only exposes VITE_-prefixed vars, and INLINES them at
# build time — so each environment needs its own build.
# Defaults to the shared ARLI backend if unset — see src/api.ts.
VITE_API_URL=https://project-af-backend.onrender.com
